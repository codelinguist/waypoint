# Waypoint frontend

React/TypeScript financial-position dashboard (WAP-15). See the repository
root `README.md` ("Frontend" section) for setup, configuration, Docker, and
test instructions — this directory has no separate setup story.
